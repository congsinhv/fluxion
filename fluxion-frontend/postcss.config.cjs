// PostCSS config — Tailwind v4 uses the @tailwindcss/vite plugin directly in Vite,
// so this file is a minimal stub kept for tooling compatibility (e.g., standalone PostCSS CLI).
module.exports = {
  plugins: {},
};
