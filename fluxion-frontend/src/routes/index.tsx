import type { JSX } from "react";

// Route tree stub — TanStack Router wired in downstream tickets.
// Replace this file with the generated routeTree.gen.ts pattern once routes are defined.
export default function RootRoute(): JSX.Element {
  return <></>;
}
